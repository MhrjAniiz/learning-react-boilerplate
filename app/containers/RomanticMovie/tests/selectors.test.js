// import { selectRomanticMovieDomain } from '../selectors';

describe('selectRomanticMovieDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
