/*
 * RomanticMovie Messages
 *
 * This contains all the text for the RomanticMovie container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RomanticMovie';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RomanticMovie container!',
  },
});
