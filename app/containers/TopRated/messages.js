/*
 * TopRated Messages
 *
 * This contains all the text for the TopRated container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.TopRated';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the TopRated container!',
  },
});
