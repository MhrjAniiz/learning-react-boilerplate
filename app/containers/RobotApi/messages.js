/*
 * RobotApi Messages
 *
 * This contains all the text for the RobotApi container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.RobotApi';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RobotApi container!',
  },
});
