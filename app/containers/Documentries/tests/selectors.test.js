// import { selectDocumentriesDomain } from '../selectors';

describe('selectDocumentriesDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
