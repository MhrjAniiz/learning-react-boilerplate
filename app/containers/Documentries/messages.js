/*
 * Documentries Messages
 *
 * This contains all the text for the Documentries container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.Documentries';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the Documentries container!',
  },
});
