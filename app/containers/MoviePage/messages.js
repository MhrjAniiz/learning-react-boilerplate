/*
 * MoviePage Messages
 *
 * This contains all the text for the MoviePage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.MoviePage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the MoviePage container!',
  },
});
