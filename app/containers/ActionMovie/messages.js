/*
 * ActionMovie Messages
 *
 * This contains all the text for the ActionMovie container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ActionMovie';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ActionMovie container!',
  },
});
