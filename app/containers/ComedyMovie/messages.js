/*
 * ComedyMovie Messages
 *
 * This contains all the text for the ComedyMovie container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ComedyMovie';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ComedyMovie container!',
  },
});
