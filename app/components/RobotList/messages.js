/*
 * RobotList Messages
 *
 * This contains all the text for the RobotList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.RobotList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the RobotList component!',
  },
});
