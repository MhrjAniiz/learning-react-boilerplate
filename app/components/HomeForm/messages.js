/*
 * HomeForm Messages
 *
 * This contains all the text for the HomeForm component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HomeForm';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HomeForm component!',
  },
});
